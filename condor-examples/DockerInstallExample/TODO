The foo.py and foo.sh files need to be updated to actually run. This will be done once we have a condor flock to run.

Also, will need to use this code:

## Get ID
ID=$(docker run -d mnfienen/amplicon_pipeline2 /bin/bash)
docker export $ID | gzip -c > tmp.tgz

## | compresses with gzip
## then import

gzip -dc tmp.tgz | docker import - mnfienen/amplicon_pipeline3
